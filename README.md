# Route Memory 3D

API en FastAPI para generar maquetas STL a partir de rutas GPX o links de actividades deportivas.

## Ejecutar localmente

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload